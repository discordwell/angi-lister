# Claudepad - Angi-Lister Session Memory

## Session Summaries

### 2026-06-18T22:00Z
Closed out the **conversion-rate** and **speed-to-lead** corners of the "one metric, computed inconsistently across pages" theme the last several passes chased. Two surgical fixes in `analytics.py`, both pinned by new `tests/test_analytics.py` cases, both verified to FAIL on pre-fix code. **(1) Removed the dead, divergent `get_conversion_funnel` conversion_rate.** The 2026-06-17 pass *explicitly flagged* it (its claudepad NOTE: "computes a `conversion_rate` … not displayed anywhere … left untouched as dead output") — an event-based `(booked + won) / (booked + won + lost)` that omits in-flight `mapped` leads, so it disagreed with the canonical `get_conversion_detail`/`get_metrics_summary` formula `(booked + won) / (mapped + booked + won + lost)` rendered on the *same* Analytics page. It was never wired to a template (the page reads `conversion.conversion_rate`), so the divergence sat latent — exactly the landmine a future caller would step on by rendering `funnel.conversion_rate`. Deleted the computation; `TestFunnelOmitsConversionRate` pins the funnel emits no `conversion_rate` key. **(2) Aligned the speed-to-lead clock-skew guard across all three surfaces.** Median seconds `lead_created`→`email_sent` is shown on the dashboard tile (`get_metrics_summary`, all-time), the Analytics funnel (`get_conversion_funnel`, windowed), and the admin per-tenant table (`get_tenant_comparison`, windowed). Only the dashboard dropped negative deltas (`sent` before `created` — impossible in normal flow, but producible by clock skew / manual edits); the two analytics surfaces lacked the guard, so one bogus row pulled their median below the dashboard's for the same data. Added `sent_ts >= created_ts` to both. `TestSpeedToLeadConsistency` builds 2 valid leads (+100s, +200s) + 1 clock-skew lead (−50s) and asserts all three report `median([100,200]) = 150.0`; verified it fails at 100.0 on the funnel/admin before the guard (dashboard already 150.0 — proving the divergence was real). How I got here: read the full service/router/worker layer, then ran two parallel audit sub-agents (metric-divergence hunt + router-correctness hunt). Most of their findings were intentional design (per-page windows are documented & sanctioned, like Delivery Rate) or verified-safe (RLS scoping in tenant_api/api_auth is correct; admin seeing `__all__` on `/console/analytics` is the same consistent RLS behavior as the dashboard; lost→won re-transitions are intentional per `test_set_outcome_allows_change`) — these two were the genuine, in-vein residue. 206 passed (was 204), 8 skipped, ruff clean. ARCHITECTURE.md: Conversion-Tracking section notes the funnel deliberately computes no rate; new Speed-to-Lead section documents the one-formula/per-page-window/clock-skew-guard convention (mirrors Delivery Rate).

### 2026-06-18T06:30Z
Fixed the **third and last** "delivery rate, one metric computed over inconsistent populations" defect — this one in `analytics.py::get_system_health`, the *only* delivery-rate computation that had **no test** (which is exactly why the last two passes' delivery-rate work missed it). `email_failure_rate_24h` (the documented complement of the canonical delivery rate, `failed / (sent + failed)`, shown on the admin analytics page and the input to `overall_health`) diverged on BOTH axes the prior passes had been aligning: **(1) simulated included** — its `email_sent_24h`/`email_failures_24h` queries had no `is_simulated == False` filter, while every sibling (`metrics.py::get_metrics_summary`, `get_tenant_comparison`, `get_personalization_performance`) excludes simulated. Because the worker forces simulated sends to `status="sent"` (email.py:180) and the operator-facing `/console/simulate` button (console.py:344) + test-lead API (api.py:176) create them, a pile of demo sends silently **inflates the denominator and dilutes a real failure spike below the `0.1` "critical" threshold** — masking an actual email outage in `overall_health` (repro: 2 real failed + 2 real sent + 16 simulated sent → true rate 0.5/"critical", but old code 2/20 = 0.1/"warn"). **(2) mismatched window anchors** — `email_sent_24h` filtered on `sent_at >= cutoff` while `email_failures_24h` filtered on `queued_at >= cutoff`, mixing two cohorts in one ratio: a message queued >24h ago but sent inside the window counted as sent, yet a sibling failure queued in the same window did not (repro: 1 sent queued 30h-ago/sent 1h-ago + 1 failed queued 1h-ago → old 1/2 = 0.5 vs correct 1/1 = 1.0). Fix: both counts now share one base query — `queued_at >= cutoff_24h AND is_simulated == False` — then add `status=="sent"`/`"failed"` (the exact `msg_q.filter(...)` reuse pattern from metrics.py:72-76; `queued_at` is the only non-null timestamp every message carries and the anchor `get_tenant_comparison` already uses). 3 new tests in `tests/test_delivery_rate.py::TestSystemHealthDeliveryPopulation` (simulated-dilution flips critical→warn; failure rate is the exact complement of `get_tenant_comparison`'s delivery rate for the same data; queued-before-window send excluded from both sides). Verified all 3 FAIL on pre-fix code (0.1 vs 0.5, 0.0667 vs 0.2, 0.5 vs 1.0 — matching the test comments) and PASS on the fix; an adversarial review sub-agent independently re-confirmed (stash/run/pop), proved the Query-reuse is non-mutating at runtime, checked no consumer depends on old behavior (only console.py:843 + analytics_admin.html), and swept for any remaining sibling with the same inconsistency (none — get_system_health was the lone outlier on both axes). 204 passed (was 201), 8 skipped, ruff clean. ARCHITECTURE.md "Delivery Rate" section now documents all three predated defects and the canonical alignment.

### 2026-06-18T01:00Z
Fixed the dashboard **"Delivery Rate"** KPI — same "one metric, two formulas, two pages" class as last session's conversion-rate fix. `metrics.py::get_metrics_summary` computed `delivery_success_rate = sent / (every non-simulated OutboundMessage)`, so the denominator counted messages that were never a completed send attempt: in-flight `pending`/`generating` and the personalization engine's intentional non-sends `declined` (job rules) / `skipped` (LLM). Effect: a queue backlog, or a tenant that declines/skips most leads, showed a spuriously low rate, and the dashboard tile (`>= 0.95 ok / >= 0.8 warn / else danger`) read **red** even when every message actually attempted was delivered. Meanwhile the admin per-tenant table (`analytics.py::get_tenant_comparison`) and `get_system_health` both use `sent / (sent + failed)` — so the dashboard was the lone outlier (repro: 1 sent/1 failed/1 declined/1 skipped/1 pending → dashboard 20% vs admin 50%, same data, identical "Delivery Rate" label). Fixed `get_metrics_summary` to `sent / (sent + failed)`, returning `None` (→ "—") when there are no terminal attempts (more honest than a red 0%). An adversarial review sub-agent then caught a SECOND, internal split: `get_tenant_comparison`'s delivery query counted **simulated** sends while its sibling `personalization_rate` query (and the dashboard) excluded them — so even after the denominator fix the two pages measured different *populations*. Aligned `delivery_rows` to filter `is_simulated == False` (matches that sibling query's own filter + its "total non-simulated messages" comment). Now both pages share one definition — terminal send outcomes, non-simulated — each applying its own by-design scope window (dashboard all-time, admin 30-day), exactly like conversion rate; "agree for the same data." 5 new tests in `tests/test_delivery_rate.py` (excludes non-attempt statuses; dashboard==admin; None when no attempts; simulated excluded from dashboard; simulated excluded from admin). Verified the 3 denominator tests FAIL on pre-fix `metrics.py` (e.g. 0.0 vs None, 0.14 vs 0.75) and the admin-population test FAILs on pre-fix `analytics.py` (0.933 vs 0.8), all PASS on the fix. 201 passed (was 196), 8 skipped, ruff clean. ARCHITECTURE.md gains a "Delivery Rate" section documenting the canonical formula + non-simulated population + per-page window.

### 2026-06-17T19:00Z
Fixed the console **Analytics** page reporting a broken, inconsistent conversion rate — three defects on the same number, all reproduced via a rendered-HTML test before fixing. Repro (1 mapped/1 booked/1 won/1 lost) rendered `<div class="metric-value danger">…0.7%`, exposing all three at once: **(1) display not scaled** — `analytics.html` rendered the 0–1 conversion *fraction* verbatim (`{{ "%.1f"|format(conversion.conversion_rate) }}%`), so `0.5` showed as `0.5%` instead of `50.0%` (two sites: the KPI tile line 20 + the outcomes-donut line 189). Every other rate in the app (`dashboard.html`, `analytics_admin.html`) multiplies by 100 — this page was the lone exception. **(2) dead color thresholds** — the tile's `{% if … > 30 %}ok{% elif … > 15 %}warn{% else %}danger` compared a value that maxes at 1.0 against 30/15, so the tile was *always* red regardless of the real rate. Fixed to fraction-scale `>= 0.30`/`>= 0.15` (matching `dashboard.html`'s `>= 0.3`). **(3) formula disagreed with the dashboard** — `analytics.py::get_conversion_detail` used `denom = booked + won + lost` (excludes in-flight `mapped` leads) while `metrics.py::get_metrics_summary` and ARCHITECTURE.md define the canonical `(booked + won) / (mapped + booked + won + lost)`. So the same data showed e.g. 66.7% on Analytics vs 50.0% on the Dashboard under an identical "Conversion Rate" label. Aligned `get_conversion_detail` to include `mapped` (= its already-computed `pipeline_total`); no test pinned the old behavior. NOTE: `get_conversion_funnel` also computes a `conversion_rate` (event-based, susceptible to double-counting a lead that went booked→won) but it is **not displayed anywhere** (analytics.html shows `conversion.conversion_rate` from `get_conversion_detail`, plus `funnel.median_speed_to_lead_seconds`/`funnel.lead_created`) and not in any schema/API — left untouched as dead output. 6 new tests in `tests/test_analytics.py`: 4 service-level (mapped-in-denominator, matches-dashboard-KPI, none-when-empty, full-rate-when-all-resolved) + 2 render-level (KPI tile shows `50.0%` not `0.5%`; tile colored `ok` not `danger` at 50%) via an authenticated console client (signed cookie + `SessionLocal` patch, mirroring `test_email_page`). Verified 4 of 6 FAIL on pre-fix code (2 pass: the no-op cases where excluding/including mapped gives the same value) then all PASS. 196 passed (was 190), 8 skipped, ruff clean. ARCHITECTURE.md conversion-tracking section updated to document the shared canonical formula + the 0–1-fraction/×100-display convention.

### 2026-06-17T14:10Z
Fixed the SAME bug class as last session's duplicate-detection fix, lurking in a sibling function the earlier pass missed: `services/personalization.py::_check_repeat_customer` (Pass 1 of the email engine). It compared **raw SQL equality** (`or_(Lead.email == lead.email, Lead.phone == lead.phone)`) while duplicate detection normalizes — so the two disagreed on "is this the same consumer," with effects in BOTH directions, both reproduced standalone before fixing: **(1) blank-field false positives** — two UNRELATED consumers who both omit a phone (valid Angi payload: `PhoneNumber`/address fields can be `""`) matched on `"" == ""`, so a stranger was returned as a "prior lead." That feeds the LLM a bogus repeat-customer context and is the gate for the SKIP decision, so it can **suppress a legitimate first-contact email** to a real new lead (worse than the duplicates export pollution — this drops outreach). (2) **false negatives from no normalization** — a true resubmission with different email casing (`Bob@X.com` vs `bob@x.com`) or phone formatting (`(555) 433-2646` vs `5554332646`) was NOT recognized as a repeat, so the consumer got a **duplicate email** even though duplicate detection (which normalizes) had already flagged the pair (repro: dup score 0.7, repeat-check priors 0). Fix: promoted `_normalize_email`/`_normalize_phone`/`_present_and_equal` → public `normalize_email`/`normalize_phone`/`present_and_equal` in `services/duplicates.py` (now the canonical "same consumer" primitives; `_normalize_address` stays private), and rewrote `_check_repeat_customer` to reuse them. Phone normalization (strip non-alphanumerics) isn't portable in SQL (SQLite has no regexp_replace), so it loads the tenant's leads inside the 7-day window and matches in Python — bounded by the window, and strictly tighter than duplicate detection's unbounded per-tenant scan. Early-returns `[]` when the lead has neither email nor phone. Preserves prior semantics (created_at desc, limit 5 applied after filtering). 5 new tests in `tests/test_personalization.py::TestRepeatCustomer` (email-case match, phone-format match, blank-phone non-match, blank-email non-match, no-identifier empty); verified all 5 FAIL on the reverted raw-equality code while the 3 pre-existing tests still pass, then PASS on the fix. 190 passed (was 185), 8 skipped, ruff clean. ARCHITECTURE.md duplicate-detection section documents the shared primitives + the repeat-customer reuse and updates the `_present_and_equal`→`present_and_equal` reference.

### 2026-06-17T07:30Z
Fixed a real correctness bug in duplicate detection plus two N+1 query patterns. **(1) Blank-field false positives** (`services/duplicates.py`): `check_duplicates` scored each component with raw equality (`ex_email == norm_email`, etc.), so a *blank* value matched another blank — `"" == ""` → True. Two UNRELATED leads (different emails) that both omit phone AND address scored phone_match (0.3) + address_match (0.3) = 0.6 ≥ 0.4 threshold and were flagged duplicates of each other; a shared-blank email even added 0.4. These false positives flow straight into the rebate-claim CSV export (`/duplicates/export`, financial impact) and pollute `duplicate_rate`. Valid Angi payloads can have blank phone/address (address fields default `""`, `Email: str` allows `""`), so it's reachable on the mapped path. Reproduced standalone (score=0.6) before fixing. Fix: new `_present_and_equal(a, b) = bool(a) and a == b` — a blank field now contributes nothing; equality already implies the other side is non-blank, so one presence check suffices. No false negatives (a real match still requires both sides to share a non-blank value). Existing dup tests unchanged (identical leads still 1.0, email-only still 0.4). **(2) N+1 queries** (`services/metrics.py`): `get_recent_leads` read `lead.tenant.name` per row (one SELECT per distinct tenant — bites the admin dashboard where leads span tenants; the ORM identity map masks it for single-tenant views) and `get_duplicate_pairs` read `m.lead`/`m.original` per row (two SELECTs per row; export calls it with limit=10000 → up to 20k queries). Added `joinedload` (two scalar many-to-one joins to the same `leads` table auto-alias safely, no cartesian/limit hazard). Dict shapes unchanged → no API/CSV contract change. 4 new tests: 2 duplicate regression guards + 2 query-count guards (`tests/test_metrics.py`, new file, counts SELECTs via a `before_cursor_execute` listener; the recent_leads test spans BOTH seeded tenants so identity-map caching can't hide the N+1). Verified each test fails on reverted code and passes on the fix. Adversarial review sub-agent independently re-confirmed (reverted each fix, watched tests fail). 185 passed (was 181), 8 skipped, ruff clean. ARCHITECTURE.md duplicate-detection section now documents the scoring + blank-field rule.

### 2026-06-17T03:15Z
Resolved the long-deferred `custom_body` intro-email rendering defect (was the standing "Known future work" item). The Jinja path (`email.py` + `templates/email/intro.{html,txt}`) rendered the tenant `intro_template` ONCE through an `autoescape=True` env and reused that string in both the HTML and text templates, causing three visible bugs — all reproduced before fixing: (1) **double-escape in HTML**: `Tom & Jerry`→`Tom &amp;amp; Jerry` (snippet escaped once, then the outer autoescaping template escaped again); (2) **whole plain-text email HTML-escaped**: the env's blanket `autoescape=True` wrongly applied to `intro.txt`, so the text email read `Co &amp; Sons` / `Hi Tom &amp; Jerry` everywhere, not just custom_body; (3) **multi-paragraph collapse**: the snippet's `\n\n` paragraphs weren't wrapped in `<p>`/`<br>`, so seeded intros rendered as one block. Discovered + fixed a 4th: **double greeting** — seeded `intro_template`s open with `Hi {{first_name}},` but the wrapper ALSO always rendered a greeting (it sat outside the if/else; the default body had none), so custom-body emails greeted twice. Fix: env now `select_autoescape(("html","xml"))` (intro.html escapes, intro.txt doesn't); new `_render_intro_snippet` renders the snippet per-context via two `overlay()` envs — HTML render escapes lead fields then `_paragraphs_to_html` adds `<p>`/`<br>` then wraps in `markupsafe.Markup` (emitted verbatim, no double-escape), text render leaves fields raw; templates consume `custom_body_html`/`custom_body_text`; wrapper greeting MOVED into the `{% else %}` branch (preserves the human-authored seed greeting per the frontend-design rule, kills the duplication). XSS invariant preserved + tested: webhook lead fields stay escaped in the HTML (the same body_html feeds the console's sandboxed lead-detail iframe). `magic_link.html` uses a SEPARATE env in `auth.py`, so this change is contained to the intro-email path. 12 new tests (`tests/test_email_render.py`, detached Lead/Tenant — render only reads attrs) covering single-escape, paragraphs, raw text, the `<script>`/`<img onerror>` XSS guard, no-double-greeting, default-body fallback, and broken-template→default fallback. 180 passed (was 168), 8 skipped, ruff clean. ARCHITECTURE.md escaping section updated.

### 2026-06-17T02:00Z
Hardened outbound-email HTML rendering against injection — the one email path that bypasses Jinja2 autoescaping. `personalize_outbound` (services/personalization.py) builds `body_html` by hand with f-strings, interpolating `lead.first_name` (straight from the Angi webhook), `tenant.name`/`phone`/`brand_color`, and the LLM-generated body — all UNescaped. A lead named `Tom & <b>x</b>` or an LLM body with markup produced invalid HTML at best, and at worst stored HTML/JS: the same `body_html` is previewed in the console via `<iframe srcdoc=...>` with NO sandbox, so injected `<script>` would execute in the operator's origin (stored XSS chain from external lead input). Fix: `html.escape()` every interpolated value (element content `quote=False` to keep apostrophes readable; attribute values keep the quote-escaping default), escaping the LLM body BEFORE the `\n\n`→`</p><p>` transform so the structural tags survive. Defense-in-depth: added `sandbox=""` to the lead_detail.html preview iframe (renders email inertly; images/CSS still display). Also escaped `send_alert`'s `<pre>{body}</pre>` (monitoring.py) — drift `extra_fields` are attacker-controlled JSON keys from the webhook. Wet-checked the render: `O'Brien & Sons HVAC`/`José`/`A&W` now emit valid escaped HTML with paragraphs intact. 3 new tests (2 personalization incl. tenant name/phone/brand_color attribute-breakout cases, 1 monitoring) that fail against the old unescaped code. 168 passed, 8 skipped, ruff clean. The Jinja2 template path (templates/email/*.html) was already safe via autoescape. A background review agent confirmed the fix + tests and flagged one PRE-EXISTING out-of-scope item (see Key Findings: custom_body double-escape) — left as documented future work rather than risk a careless `|safe` fix in the decommissioned email path; added warning comments at both sites (intro.html + email.py) instead.

### 2026-06-11T00:00Z
Landed the previously-verified-but-uncommitted naive-UTC/lint/SSE pass (commit 8852178), then hardened the ingestion edges. (1) Webhook body capture: `await request.json()` on invalid JSON (or a non-dict body → `raw_body.get` AttributeError) was an unhandled 500 — NO receipt captured, and Angi would retry; violates the receipts-are-first-class contract. New `_capture_body()` stores the literal text under `{"_raw_body": ...}` (10KB cap), marks parse_valid=False, emits parse_failed, ACKs `<success>`. Deduped both endpoints through `_record_parse_failure()`. Non-string CorrelationId no longer written to the String column (PG type error). (2) X-API-KEY now `hmac.compare_digest` on **encoded bytes** — str compare_digest raises TypeError on non-ascii (latin-1 header probe → 500); test sends header value as latin-1 bytes because httpx refuses non-ascii str headers. (3) Worker: crash in `process_outbound_message` rolled back the attempts increment too → message stayed pending/attempts=0, hot-retried every poll forever. `_record_crashed_attempt()` re-counts after rollback, marks failed + email_failed event at MAX_ATTEMPTS. (4) TESTING GOTCHA: the conftest `db` fixture's connection-joined session does NOT savepoint on rollback — `db.rollback()` inside code-under-test discards the ENTIRE fixture state (commits are flush-only). Any test exercising real commit/rollback (run_cycle) must use the real engine like test_session_hygiene (new `worker_db` fixture in test_worker.py). (5) Post-commit review agent caught 2 real bugs in my `_capture_body`: decoding to str before json.loads REJECTS UTF-8 BOMs that `request.json()` (bytes parse, stdlib auto-detect) accepted — BOM'd valid leads were silently dropped WITH retries suppressed; and RecursionError (deep nesting) / UnicodeDecodeError (non-UTF-8) escaped the except → still 500-with-no-receipt. Fixed: parse bytes, widen except. Lesson: when wrapping a parser, match the original's input type exactly. Also: pre-commit API-key check was plain `!=` (never 500'd on non-ascii — compare_digest-on-bytes is hardening, not a bugfix; first commit message overclaims). 165 passed, 8 skipped.

### 2026-06-10T03:40Z
Committed the previously-uncommitted RLS session-lifecycle WIP (verified 150 passed first), then a code-health pass: (1) unified the datetime convention on naive UTC via new `app/utils.py::utcnow()` — 13 model defaults, all services/routers/worker, and `services/auth.py::_utcnow` now share it; removed the `geocoding.py` force-aware hack (`expires_at.replace(tzinfo=dt.UTC)`) that papered over the mixed convention. Both backends read naive (SQLite's dialect silently DROPS "+00:00" offsets on parse — aware writes were lies), so aware `now()` values were the only odd ones out. New `tests/test_time_convention.py` enforces it (mapper-default scan + source scan). (2) Fixed all 32 ruff findings, FastAPI `regex=`→`pattern=` deprecation, pytest-asyncio loop-scope warning — suite now warning-free. (3) Added `?once=1` to `/console/events` SSE endpoint (single poll then close): an infinite-stream test via TestClient `client.stream()` HANGS on context teardown — use `once` for SSE tests, never stream-context reads. New SSE smoke test (real topology, signed console cookie built via `_sign_cookie`). 154 passed, 8 skipped.

### 2026-06-10T03:05Z
Landed the WIP capture-before-commit fix in tenant_api.py and finished the RLS session-lifecycle work behind it. Root problems: (1) `require_tenant`/`require_admin` created `SessionLocal()` sessions that were never closed (connection leak — plain-return deps have no teardown); (2) `PUT /api/v1/tenant/config` mutated `ctx.tenant` (owned by the auth/bypass session) but committed `ctx.db`, so config updates were silently rolled back in prod — masked in tests because overrides unified all sessions; (3) `last_used_at` was flushed on the auth session but never committed; (4) session-scoped `SET app.current_tenant` persisted on pooled connections (cross-request leak hazard; worker only worked because a single-threaded pool hands back the same connection). Fixes: `set_tenant(session_scope=True)` now installs an `after_begin` listener that re-issues `SET LOCAL` per transaction (nothing persists on the connection; worker/console/bypass survive mid-work commits structurally); `require_tenant`/`require_admin` are yield-deps that close their scoped session and commit the auth session post-auth; config update applies to the `ctx.db` tenant row and builds the response pre-commit; seed pins bypass session-scoped. New tests/test_session_hygiene.py runs WITHOUT dependency overrides (real session topology, file-backed sqlite) — the config-persistence test fails on pre-fix code. test_tenant_api/test_admin_api fixtures now patch `SessionLocal` to fresh sessions on the fixture connection (the old `lambda: db` relied on the leak). 150 passed, 8 skipped (PG-gated, incl. 2 new scoping-semantics tests).

### 2026-05-19T02:35Z
**Decommissioned.** Project sat idle since 2026-04-06 demo (64 leads, all from that day). Investigated the ongoing `volume_anomaly` warning — turned out to be the canary working correctly: `check_volume_anomaly` fires when leads exist but none arrived in 6h, which was simply true. No emails were being sent (ALERT_EMAIL unset in prod), just log lines. User opted to decommission rather than tune the check. Stopped containers via `docker compose -f docker-compose.prod.yml down` on ovh2, removed `/etc/caddy/sites/angi.discordwell.com`, reloaded Caddy. `/opt/angi-lister` directory and `pgdata` volume preserved on server. README updated with decommission notice. To revive: re-add Caddy site, run `./infra/deploy.sh`.

### 2026-05-19T02:20Z
Fixed `angi-lister-worker-1` reporting unhealthy in production (47k failing streak since deploy). Root cause: Dockerfile bakes in `HEALTHCHECK curl localhost:8000/healthz` which is correct for the api but inherited by the worker, which doesn't serve HTTP. Fix: added `healthcheck: disable: true` to worker service in docker-compose.prod.yml. `restart: unless-stopped` still handles real crashes. Deployed; worker now reports plain "Up" with no health status. Worker functionally was fine all along — daily monitoring heartbeats continue, including ongoing `volume_anomaly` warning worth a separate look.

### 2026-04-06T23:10Z
Added two stretch features: (1) Conversion feedback loop — POST /api/v1/leads/{id}/outcome and console UI with booked/won/lost buttons, conversion_rate KPI on dashboard. (2) Schema drift monitoring — new app/services/monitoring.py with error rate checks, schema drift aggregation, volume anomaly detection, debounced alerting via Resend, daily health check in worker loop, GET /api/v1/health/schema endpoint. Added 27 new tests (test_conversion.py, test_monitoring.py). Fixed _set_tenant -> set_tenant import in api_auth.py. All 103 tests pass. Config: ALERT_EMAIL, ALERT_ERROR_THRESHOLD, ALERT_WINDOW_MINUTES.

### 2026-04-06T22:10Z
Implemented PostgreSQL Row-Level Security (RLS) for multi-tenant architecture. Added `tenant_id` to WebhookReceipt, LeadEvent, DuplicateMatch. Created migration 004 with FORCE ROW LEVEL SECURITY + tenant_isolation policies on 11 tables. Three access modes: `__bypass__` (webhook/worker/system), `__all__` (admin), `{tenant_uuid}` (tenant user). Console routes now use tenant-scoped sessions from ConsoleSession.tenant_id. Refactored all routers: webhook/api/auth/health use get_bypass_db, console uses get_console_db. Worker and seed script set bypass. 53 SQLite tests pass. Deployed to prod, applied RLS policies, verified webhook + health working. Paschal Air is demo tenant.

### 2026-04-06T21:30Z
Replaced fictional seed tenants (Apex HVAC, BlueWave Plumbing, Spark Electric) with real Netic customers (Hoffmann Brothers, Paschal Air, Heartland Home Services). Updated seed script, test fixtures, simulate script, console template, and README. All 26 tests pass. Deployed to production with `--reset` reseed.

## Key Findings

- **Netic customers (public):** Hoffmann Brothers (St. Louis HVAC), Paschal Air Plumbing & Electric (NW Arkansas), Heartland Home Services (Midwest HVAC)
- **Production DB requires `--reset` flag** when seed data structure changes — but `--reset` drops RLS policies. Must re-run `alembic upgrade head` or re-apply policies manually after reset.
- **Mapping mechanism:** ALAccountId field in Angi payload -> angi_account_mappings table -> tenant_id. Unmapped leads are stored but don't trigger outbound emails.
- **RLS bypass on SQLite:** `_set_tenant` no-ops on non-PostgreSQL dialects (checked via `db.get_bind().dialect.name`). Existing SQLite tests work unchanged.
- **Test fixtures must override all DB deps:** `get_db`, `get_bypass_db`, `get_admin_db`, `get_console_db` all need overrides in conftest for SQLite tests.
- **Admin login:** cordwell@gmail.com / admin — console session with `tenant_id=None` gets `__all__` mode.
- **Demo login:** Paschal Air via `/auth/demo-login` — console session with specific `tenant_id` gets RLS-scoped views.
- **FIXED 2026-06-17 — duplicate-detection blank-field false positives:** `check_duplicates` (`services/duplicates.py`) compared components with raw equality, so blank `==` blank counted as a match — two unrelated leads both missing phone+address scored 0.6 and were flagged duplicates, contaminating the rebate-claim CSV export. Guarded with `present_and_equal` (a blank field contributes nothing). Also eager-loaded the N+1 relationships in `get_recent_leads`/`get_duplicate_pairs` (`services/metrics.py`). Regression + query-count tests in `tests/test_duplicates.py` and new `tests/test_metrics.py`.
- **FIXED 2026-06-17 (later) — repeat-customer check had the SAME bug class:** `_check_repeat_customer` (`services/personalization.py`, Pass 1) used raw SQL equality while duplicate detection normalizes. Effect both ways: blank `"" == ""` made unrelated strangers look like repeat customers (can SKIP → suppresses a legitimate first email), and no normalization missed true resubmissions with different email-case/phone-format (→ duplicate email sent). Now reuses the canonical `normalize_email`/`normalize_phone`/`present_and_equal` from `duplicates.py` (promoted to public this session). Loads tenant leads in the 7-day window and matches in Python (phone normalization isn't portable SQL). Tests: `tests/test_personalization.py::TestRepeatCustomer`. **Lesson:** when fixing a bug, grep for the same anti-pattern (`Lead.email == ... / Lead.phone == ...` raw equality) across sibling modules — this one was a copy of the duplicates bug.
- **FIXED 2026-06-17 (analytics) — conversion rate broken + inconsistent on the Analytics page:** `analytics.html` displayed the 0–1 conversion fraction without `* 100` (`0.5` → `0.5%`) at two sites (KPI tile + outcomes donut), and colored the tile with percentage-scale thresholds (`> 30`/`> 15`) it could never reach → always red. Also `analytics.py::get_conversion_detail` excluded `mapped` leads from the denominator, disagreeing with the dashboard/`get_metrics_summary`/ARCHITECTURE.md canonical `(booked + won) / (mapped + booked + won + lost)`. Fixed display (`* 100`, fraction thresholds `>= 0.30`/`>= 0.15`) + formula (include `mapped`). Tests: `tests/test_analytics.py`. **Convention:** all `*_rate` values are 0–1 fractions in the service layer; templates scale `* 100` for display and use fraction-scale thresholds (see `analytics_admin.html`, `dashboard.html`). **Lesson:** when a rate renders, check it's scaled AND its color thresholds are on the same scale — and that every place computing the "same" KPI shares one formula. `get_conversion_funnel.conversion_rate` is dead (not displayed) and was left as-is.
- **RESOLVED 2026-06-17 — `custom_body` email rendering:** The tenant `intro_template` is now rendered separately per context by `email.py::_render_intro_snippet` (HTML escaped + `<p>`/`<br>`-wrapped `Markup`; text raw); the email env uses `select_autoescape(("html","xml"))` so `intro.txt` no longer HTML-escapes; templates consume `custom_body_html`/`custom_body_text`; the wrapper greeting moved into the `{% else %}` branch to stop double-greeting. The webhook-untrusted-field escaping (XSS guard for the console iframe preview) is preserved and covered by `tests/test_email_render.py`. The earlier double-escape was XSS-SAFE; the fix made it correct without a careless `|safe` by escaping in the inner render and wrapping in `Markup`.
- **FIXED 2026-06-18 — dashboard "Delivery Rate" denominator + simulated split (SAME class as conversion rate):** `metrics.py::get_metrics_summary.delivery_success_rate` divided by ALL non-simulated messages, counting in-flight `pending`/`generating` and intentional non-sends `declined`/`skipped` as delivery failures (→ spuriously low rate, red tile even when every attempted send succeeded), while `analytics.py::get_tenant_comparison` and `get_system_health` use `sent / (sent + failed)`. Separately, `get_tenant_comparison` counted *simulated* sends in its delivery rate but excluded them from its sibling personalization rate (and the dashboard excluded them) — so the two pages also differed on row *population*. Fixed dashboard to `sent / (sent + failed)` (None→"—" when no attempts) and added `is_simulated == False` to `get_tenant_comparison`'s delivery query. Canonical "delivery rate" = terminal send outcomes (`sent + failed`), non-simulated; each page keeps its own by-design scope window (dashboard all-time, admin 30-day). Tests: `tests/test_delivery_rate.py` (5). **Lesson (reinforces the conversion-rate one):** when a rate renders on two pages, a shared *formula* isn't enough — confirm the denominator composition AND the row population (time window, `is_simulated`) match.
- **FIXED 2026-06-18 (later) — `get_system_health` email failure rate, the THIRD delivery-rate population split (and the one with no test):** `analytics.py::get_system_health.email_failure_rate_24h` (admin page + `overall_health` input) is the documented complement of delivery rate but (1) counted *simulated* sends in its denominator — demo traffic from `/console/simulate` dilutes a real failure spike below the `0.1` "critical" threshold, masking an email outage — and (2) anchored `email_sent_24h` on `sent_at` while `email_failures_24h` used `queued_at`, mixing two 24h cohorts in one ratio. Fixed: one base query `queued_at >= cutoff_24h AND is_simulated == False`, then split by `status` (reuses metrics.py's `msg_q.filter(...)` pattern). All four message-rate sites now agree on population (non-simulated) + anchor (`queued_at`). Tests: `tests/test_delivery_rate.py::TestSystemHealthDeliveryPopulation` (3). **Lesson:** the "untested computation" is where the same-class bug hides after siblings are fixed — when aligning a KPI across functions, grep for EVERY function returning that metric (incl. its complement/health-rollup forms), not just the ones with existing tests. `email_failures_24h` was the lone outlier on BOTH the `is_simulated` and time-anchor axes.
