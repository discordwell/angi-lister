# Claudepad - Angi-Lister Session Memory

## Session Summaries

### 2026-06-11T00:00Z
Landed the previously-verified-but-uncommitted naive-UTC/lint/SSE pass (commit 8852178), then hardened the ingestion edges. (1) Webhook body capture: `await request.json()` on invalid JSON (or a non-dict body → `raw_body.get` AttributeError) was an unhandled 500 — NO receipt captured, and Angi would retry; violates the receipts-are-first-class contract. New `_capture_body()` stores the literal text under `{"_raw_body": ...}` (10KB cap), marks parse_valid=False, emits parse_failed, ACKs `<success>`. Deduped both endpoints through `_record_parse_failure()`. Non-string CorrelationId no longer written to the String column (PG type error). (2) X-API-KEY now `hmac.compare_digest` on **encoded bytes** — str compare_digest raises TypeError on non-ascii (latin-1 header probe → 500); test sends header value as latin-1 bytes because httpx refuses non-ascii str headers. (3) Worker: crash in `process_outbound_message` rolled back the attempts increment too → message stayed pending/attempts=0, hot-retried every poll forever. `_record_crashed_attempt()` re-counts after rollback, marks failed + email_failed event at MAX_ATTEMPTS. (4) TESTING GOTCHA: the conftest `db` fixture's connection-joined session does NOT savepoint on rollback — `db.rollback()` inside code-under-test discards the ENTIRE fixture state (commits are flush-only). Any test exercising real commit/rollback (run_cycle) must use the real engine like test_session_hygiene (new `worker_db` fixture in test_worker.py). (5) Post-commit review agent caught 2 real bugs in my `_capture_body`: decoding to str before json.loads REJECTS UTF-8 BOMs that `request.json()` (bytes parse, stdlib auto-detect) accepted — BOM'd valid leads were silently dropped WITH retries suppressed; and RecursionError (deep nesting) / UnicodeDecodeError (non-UTF-8) escaped the except → still 500-with-no-receipt. Fixed: parse bytes, widen except. Lesson: when wrapping a parser, match the original's input type exactly. Also: pre-commit API-key check was plain `!=` (never 500'd on non-ascii — compare_digest-on-bytes is hardening, not a bugfix; first commit message overclaims). 165 passed, 8 skipped.

### 2026-06-10T03:40Z
Committed the previously-uncommitted RLS session-lifecycle WIP (verified 150 passed first), then a code-health pass: (1) unified the datetime convention on naive UTC via new `app/utils.py::utcnow()` — 13 model defaults, all services/routers/worker, and `services/auth.py::_utcnow` now share it; removed the `geocoding.py` force-aware hack (`expires_at.replace(tzinfo=dt.UTC)`) that papered over the mixed convention. Both backends read naive (SQLite's dialect silently DROPS "+00:00" offsets on parse — aware writes were lies), so aware `now()` values were the only odd ones out. New `tests/test_time_convention.py` enforces it (mapper-default scan + source scan). (2) Fixed all 32 ruff findings, FastAPI `regex=`→`pattern=` deprecation, pytest-asyncio loop-scope warning — suite now warning-free. (3) Added `?once=1` to `/console/events` SSE endpoint (single poll then close): an infinite-stream test via TestClient `client.stream()` HANGS on context teardown — use `once` for SSE tests, never stream-context reads. New SSE smoke test (real topology, signed console cookie built via `_sign_cookie`). 154 passed, 8 skipped.

### 2026-06-10T03:05Z
Landed the WIP capture-before-commit fix in tenant_api.py and finished the RLS session-lifecycle work behind it. Root problems: (1) `require_tenant`/`require_admin` created `SessionLocal()` sessions that were never closed (connection leak — plain-return deps have no teardown); (2) `PUT /api/v1/tenant/config` mutated `ctx.tenant` (owned by the auth/bypass session) but committed `ctx.db`, so config updates were silently rolled back in prod — masked in tests because overrides unified all sessions; (3) `last_used_at` was flushed on the auth session but never committed; (4) session-scoped `SET app.current_tenant` persisted on pooled connections (cross-request leak hazard; worker only worked because a single-threaded pool hands back the same connection). Fixes: `set_tenant(session_scope=True)` now installs an `after_begin` listener that re-issues `SET LOCAL` per transaction (nothing persists on the connection; worker/console/bypass survive mid-work commits structurally); `require_tenant`/`require_admin` are yield-deps that close their scoped session and commit the auth session post-auth; config update applies to the `ctx.db` tenant row and builds the response pre-commit; seed pins bypass session-scoped. New tests/test_session_hygiene.py runs WITHOUT dependency overrides (real session topology, file-backed sqlite) — the config-persistence test fails on pre-fix code. test_tenant_api/test_admin_api fixtures now patch `SessionLocal` to fresh sessions on the fixture connection (the old `lambda: db` relied on the leak). 150 passed, 8 skipped (PG-gated, incl. 2 new scoping-semantics tests).

### 2026-05-19T02:35Z
**Decommissioned.** Project sat idle since 2026-04-06 demo (64 leads, all from that day). Investigated the ongoing `volume_anomaly` warning — turned out to be the canary working correctly: `check_volume_anomaly` fires when leads exist but none arrived in 6h, which was simply true. No emails were being sent (ALERT_EMAIL unset in prod), just log lines. User opted to decommission rather than tune the check. Stopped containers via `docker compose -f docker-compose.prod.yml down` on ovh2, removed `/etc/caddy/sites/angi.discordwell.com`, reloaded Caddy. `/opt/angi-lister` directory and `pgdata` volume preserved on server. README updated with decommission notice. To revive: re-add Caddy site, run `./infra/deploy.sh`.

### 2026-05-19T02:20Z
Fixed `angi-lister-worker-1` reporting unhealthy in production (47k failing streak since deploy). Root cause: Dockerfile bakes in `HEALTHCHECK curl localhost:8000/healthz` which is correct for the api but inherited by the worker, which doesn't serve HTTP. Fix: added `healthcheck: disable: true` to worker service in docker-compose.prod.yml. `restart: unless-stopped` still handles real crashes. Deployed; worker now reports plain "Up" with no health status. Worker functionally was fine all along — daily monitoring heartbeats continue, including ongoing `volume_anomaly` warning worth a separate look.

### 2026-04-06T23:10Z
Added two stretch features: (1) Conversion feedback loop — POST /api/v1/leads/{id}/outcome and console UI with booked/won/lost buttons, conversion_rate KPI on dashboard. (2) Schema drift monitoring — new app/services/monitoring.py with error rate checks, schema drift aggregation, volume anomaly detection, debounced alerting via Resend, daily health check in worker loop, GET /api/v1/health/schema endpoint. Added 27 new tests (test_conversion.py, test_monitoring.py). Fixed _set_tenant -> set_tenant import in api_auth.py. All 103 tests pass. Config: ALERT_EMAIL, ALERT_ERROR_THRESHOLD, ALERT_WINDOW_MINUTES.

### 2026-04-06T22:10Z
Implemented PostgreSQL Row-Level Security (RLS) for multi-tenant architecture. Added `tenant_id` to WebhookReceipt, LeadEvent, DuplicateMatch. Created migration 004 with FORCE ROW LEVEL SECURITY + tenant_isolation policies on 11 tables. Three access modes: `__bypass__` (webhook/worker/system), `__all__` (admin), `{tenant_uuid}` (tenant user). Console routes now use tenant-scoped sessions from ConsoleSession.tenant_id. Refactored all routers: webhook/api/auth/health use get_bypass_db, console uses get_console_db. Worker and seed script set bypass. 53 SQLite tests pass. Deployed to prod, applied RLS policies, verified webhook + health working. Paschal Air is demo tenant.

### 2026-04-06T21:30Z
Replaced fictional seed tenants (Apex HVAC, BlueWave Plumbing, Spark Electric) with real Netic customers (Hoffmann Brothers, Paschal Air, Heartland Home Services). Updated seed script, test fixtures, simulate script, console template, and README. All 26 tests pass. Deployed to production with `--reset` reseed.

## Key Findings

- **Netic customers (public):** Hoffmann Brothers (St. Louis HVAC), Paschal Air Plumbing & Electric (NW Arkansas), Heartland Home Services (Midwest HVAC)
- **Production DB requires `--reset` flag** when seed data structure changes — but `--reset` drops RLS policies. Must re-run `alembic upgrade head` or re-apply policies manually after reset.
- **Mapping mechanism:** ALAccountId field in Angi payload -> angi_account_mappings table -> tenant_id. Unmapped leads are stored but don't trigger outbound emails.
- **RLS bypass on SQLite:** `_set_tenant` no-ops on non-PostgreSQL dialects (checked via `db.get_bind().dialect.name`). Existing SQLite tests work unchanged.
- **Test fixtures must override all DB deps:** `get_db`, `get_bypass_db`, `get_admin_db`, `get_console_db` all need overrides in conftest for SQLite tests.
- **Admin login:** cordwell@gmail.com / admin — console session with `tenant_id=None` gets `__all__` mode.
- **Demo login:** Paschal Air via `/auth/demo-login` — console session with specific `tenant_id` gets RLS-scoped views.
